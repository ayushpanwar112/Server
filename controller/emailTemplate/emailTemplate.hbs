<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Stockbox Email</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #e9f9ec;
    }
    .container {
      max-width: 600px;
      margin: 30px auto;
      background-color: #ffffff;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .header {
      background-color: #039016;
      color: white;
      padding: 20px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
    }
    .content {
      padding: 24px;
      color: #333;
    }
    .content p {
      margin: 10px 0;
      line-height: 1.6;
    }
    .highlight {
      color: #039016;
      font-weight: bold;
    }
    .footer {
      background-color: #f5f5f5;
      padding: 20px;
      text-align: center;
      font-size: 14px;
      color: #555;
    }
    .footer strong {
      display: block;
      margin-top: 10px;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>{{title}}</h1>
    </div>
    <div class="content">
      <p><strong>From:</strong> {{email}}</p>
      <p><strong>Name:</strong> <span class="highlight">{{name}}</span></p>
      <p><strong>Subject:</strong> {{subject}}</p>
      <p><strong>Message:</strong></p>
      <p>{{{message}}}</p>

      {{#if phone}}
      <p><strong>Contact:</strong> {{phone}}</p>
      {{/if}}
    </div>
    <div class="footer">
      <p>Thank you for using our service.</p>
      <strong>Stockbox Support</strong>
      <div>📞 7217019005, 7217019001</div>
    </div>
  </div>
</body>
</html>
